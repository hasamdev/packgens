<?php
/**
 * Second call to action band.
 *
 * Same component as the first; separate content so a page can carry both.
 *
 * @package Packgens
 */

defined( 'ABSPATH' ) || exit;

get_template_part( 'template-parts/sections/cta', null, $args );
